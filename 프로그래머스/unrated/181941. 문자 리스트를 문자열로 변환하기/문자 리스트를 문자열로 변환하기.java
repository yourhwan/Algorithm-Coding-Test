class Solution {
    public String solution(String[] arr) {
        String answer = "";
        
        for (int i=0; i<arr.length; i++) {
            answer += arr[i];
            // .length()의 경우 문자열의 길이를 그리고 .length 는 배열의 길이를 반환해준다.
        }
        
        return answer;
    }
}